spring:
  sql:
    init:
      schema-locations:
        -classpath*: schema-all.sql
      data-locations:
        -classpath*: data-all.sql
      username: root
      password: 1234
      mode: always